# Areta

Personal execution and weekly-regeneration platform (formerly "LifeOS" — renamed mid-build; `CLAUDE.md` still refers to the product as LifeOS throughout since that spec file is intentionally kept unchanged as phases ship, not because the rename didn't happen). Full product spec, phase plan, and Claude Code development rules live in [`CLAUDE.md`](./CLAUDE.md) — that file is the source of truth for *what* this product is and *why*. This README covers the practical side: current build status, local setup, and how to work in this repo day to day.

- **Live:** https://areta-ai.com (also reachable at https://getlifeos.tech and https://lifeos-eosin-nine.vercel.app during the domain transition — see "Areta rebrand" status entry below)
- **Repo:** https://github.com/dgray4224/lifeos (repo itself not yet renamed on GitHub)

## Status

**Phase 0 (Foundation) + Phase 1 (Onboarding and Personal OS): done**, deployed, and verified against the real Supabase project — signup, branded email confirmation, onboarding (Identity → Goals → Nutrition → Exercise → Review for the V1 health path), review/confirm, and a personalized dashboard all work end to end, in production, with real transactional email. Onboarding was later trimmed (see the "Onboarding trim" status entry below) to cut churn risk from a long upfront interview; Sleep and Coaching are no longer onboarding steps, and Recovery/Learning stay reachable in the data model for a future re-introduction of fine-grained goal areas but aren't offered by the current single "Health" chip in the Goals step.

**Phase 2 (Today screen and daily logging): done**, deployed, and verified in production. The dashboard is now a daily working screen: create/complete/skip tasks (with status history via `action_events`), a next-action recommendation, and quick-log entry for all five founder-relevant log types — weight, sleep, nutrition, recovery, and learning/study sessions.

**Phase 3 (Outcome-to-Operating-Parameters engine, meal planning, grocery list, Sunday prep): done**, deployed, and verified in production. A deterministic nutrition engine (Mifflin-St Jeor BMR, activity multipliers, safe rate-of-change clamping, calorie floors) turns onboarding answers into calorie/protein/macro targets with full assumption/rationale/confidence disclosure, requiring explicit user approval before anything downstream uses them (`/plan/parameters`). Approved targets drive a rule-based 7-day meal plan generator over a seeded 24-recipe library (`/plan/meals`), which cascades on approval into an aggregated, inventory-subtracted, section-grouped grocery list (`/plan/grocery`) and a Sunday prep plan following CLAUDE.md's exact 10-step ordered workflow (`/plan/prep`). No AI calls anywhere in this phase — all of it is deterministic, per the "Day 3-4" scope in the phase schedule; structured AI generation is Phase 4's weekly regeneration engine.

**Phase 4 (Weekly Review and Regeneration): done**, deployed, and verified in production with a real AI call. `/review` computes this week's deterministic metrics (weight change, calorie/protein adherence, sleep, pain/swelling trend, task completion, missed-task reasons, a data-sparsity flag) and calls a real `AIProvider` (Anthropic, via forced tool-use structured output validated against a Zod schema) to generate the qualitative half of a Weekly Operating Brief — executive summary, per-goal progress classification, top 3 priorities, proposed changes with reasons, risks, a weekly motivating quote, and the highest-leverage action (`/review/brief`). The AI never invents numbers or medical guidance: nutrition targets and the meal/grocery/prep plan stay on Phase 3's deterministic generators, and the system prompt explicitly forbids suggesting changes to brace settings, weight-bearing, exercise intensity, or medication. Approving the brief cascades the full regeneration cycle — nutrition parameters recalculate from the latest logged weight and auto-approve as a bundle, the meal plan regenerates folding in never-recommend preferences, grocery/prep cascade as in Phase 3, and `weekly_outcomes` rolls forward using the brief's priorities.

This closes out the phases in CLAUDE.md §6. See `CLAUDE.md` for the full spec if further work continues past this MVP.

**Design pass (logo + data-oriented dashboard): done**, deployed, and verified in production, later superseded by the terracotta rebrand below. The dashboard's Trends section (`app/(app)/dashboard/trends-data.ts`, `platform/ui/charts/*`) renders real charts from actual logged data — weight (7-day moving average as the accent line), sleep duration, nutrition calories vs. target, task completion rate, and recovery pain/swelling — built with `recharts`. Both light and dark mode confirmed live in the browser.

**Settings (Profile / Personalization / Appearance / Account): done**, deployed, and verified in production. `/settings/profile` edits the same identity fields captured during onboarding without redoing the interview. `/settings/personalization` — Coaching preferences (tone/planning style/reminders/explanation depth/never-recommend) and Work & School context, both fully optional and editable anytime, having moved out of onboarding. `/settings/appearance` has a real System/Light/Dark theme picker — Tailwind's `dark:` variant reads a `.dark` class (`platform/theme/theme.ts`) instead of `prefers-color-scheme` directly, set by a blocking pre-hydration script (zero flash) and kept live on "System" by `ThemeSync`. `/settings/account` has a real JSON data export, a confirm-gated "Restart onboarding" action, and Sign out (moved here once the mobile top-nav dropdown was removed — see the mobile nav entry below).

**Onboarding trim + Durable Memory + Smart Contextual Prompts: done**, deployed, and verified against the real Supabase project (migration `0011` pushed). Onboarding is now Identity → Goals → Nutrition → Exercise → Review for the V1 health path (down from 7 steps) — Sleep and Coaching were removed as onboarding questions (Sleep stays a trackable/loggable domain; Coaching now lives in `/settings/personalization`, defaulted to gentle/flexible/minimal/brief until changed), and the Goals form was cut from 10 fields per goal to 4 (Goal, Target date, Related area, Priority). The weekly review's static 8-question form is gone too — CLAUDE.md §7 Layer 4 "Durable Memory" (spec'd from the start, never built until now) exists for real: a `memories` table (preference/constraint/successful_strategy/failed_strategy/stable_schedule/motivation/communication_preference, each with evidence/confidence/review-date/user-confirmed) plus a `prompt_events` table for cooldown tracking. A fixed, deterministic trigger catalog (`domains/prompts/triggers.ts` — code decides when to ask, per CLAUDE.md rule 6, never the AI) evaluates conditions like a stalled exercise goal, a goal whose target date has passed, sleep trending below a floor, or a repeated pattern of skipped required tasks in one domain; at most one fires at a time, shown as a single dismissible card on the dashboard, and an answered prompt becomes a memory. `generateWeeklyBrief` reads recent memories instead of the removed weekly-answers.

**Today/Plan screens redesigned for prospective execution: done**, deployed, and verified in production. The old Today screen led with a 30-day retrospective chart; it's now demoted to the bottom under "This month," replaced at the top by a goal-linked task list (each task shows which goal it serves), today's workout, and a "this week" quick-links strip (grocery/meal/workout/prep status) so the two most-needed answers — "what do I do today" and "where's my grocery list" — are immediate instead of buried in a 6-step wizard. `/plan` is now a Week/Month/Year-tabbed view (`Month`/`Year` honestly render "not built yet" rather than fake data); the Week tab is a real 7-day calendar grid (meals + workout per day, today's box outlined) with a week picker for browsing past weeks, plus an AI-*selected* (never AI-generated) weekly motivating quote — the model picks an id from a small curated, hand-verified quote bank (`domains/motivation/quotes.ts`) rather than freely recalling a quote, which makes a fabricated or misattributed quote structurally impossible. The old 6-step generation wizard still exists, relocated to `/plan/setup`. Also fixed during this pass: the Sunday-prep time estimate summed every recipe's cook time as if cooked in series; it now only counts the single longest cook time, since the generated steps already tell the user to cook things in parallel.

**Today screen iteration (chart colors, date-selectable history, real insights): done**, deployed, and verified in production. Every chart's fill color was still the pre-rebrand blue (`platform/ui/charts/colors.ts` never got touched during the terracotta rebrand) — replaced with the brand terracotta, validated CVD-safe with the dataviz skill's palette checker against the categorical pair used on the two-series Recovery chart (a same-hue second series would have failed colorblind-safety checks, so that slot stays a cool blue rather than another orange). The Today day-strip is now a real date picker, not decoration: `getDashboardData(userId, viewDate)` takes an optional date and every day-scoped query (tasks, logs, meals, workout) keys off it; clicking a day shows that day's real data with a "Viewing [date] · Jump to today" banner, while today's cell keeps a distinct outline even when a different day is selected. The "Current phase" card (redundant with the section-tab active state) is gone, replaced by a one-line subtitle under the greeting; the "Next action" hero — which the founder pointed out was showing generic filler almost all the time — now surfaces a real computed insight: the next actionable task with a "moves you toward: {goal}" reason line pulled from the existing goal join, or, when nothing's actionable, a deterministic 7-day weight-trend delta (`domains/weight/trend.ts`'s `computeRecentWeightDelta`, unit-tested) or a "required tasks done" note instead of dead text. Workout and Planned meals now sit side by side above "This week" (was below); the meals list switched from a single-line `flex justify-between` row to a stacked label-over-value layout after long recipe names started colliding with the meal-type label once the card went to half-width.

**Terracotta brand identity + mobile-first navigation: done**, deployed, and verified in production, in both light and dark mode. New design tokens (`app/globals.css` — warm cream canvas, near-black/rich-brown hero surfaces depending on theme, terracotta brand color) and shared `Card`/`Button`/icon primitives (`platform/ui/`) replace the old plain-neutral bordered-box styling on the Today and Plan screens. A bottom tab bar (`app/(app)/BottomTabBar.tsx`) now handles primary navigation on phone **and tablet** widths (up to Tailwind's `xl` breakpoint, 1280px — an iPad's portrait viewport is 768px+, wider than the more typical `sm`/640px phone-only cutoff, so the breakpoint was deliberately raised to actually cover tablets); desktop keeps a top nav. A center FAB opens a quick-log sheet linking to all five log routes.

**Forgot password flow: done**, deployed, and verified in production (a real reset email was sent and completed end-to-end during testing). Uses Supabase's built-in `resetPasswordForEmail` + `updateUser` — reuses the existing `/auth/confirm` route (already generic over PKCE code / token_hash verification) rather than needing a second callback route. New `/forgot-password` and `/reset-password` pages; `/reset-password` is deliberately excluded from the middleware's auth-route redirect list, since the temporary recovery session it depends on would otherwise get redirected away before the user can set a new password.

**Areta rebrand: essentially done**, deployed, and verified in production. Product renamed from LifeOS to **Areta** (Greek ἀρετή/*arete* — excellence, fulfilling one's potential; tagline "Become more of who you are"). Logo mark (`app/icon.svg`, `platform/ui/Logo.tsx` — terracotta rounded-square with a dark cut-out "A," the crossbar gap reading as an upward arrow) and wordmark; domain `areta-ai.com` live on Vercel and fully wired through Resend (email-sending domain verified) and Supabase (Site URL + Redirect URLs + SMTP sender updated); Vercel and Supabase dashboard project names updated; the tagline now sits next to the wordmark in the app header (`| Become more of who you are`, hidden below `sm` to keep the mobile header uncluttered). A later full-repo text sweep (32 files) caught every remaining "LifeOS" string the initial rebrand missed — onboarding/settings/plan copy, the AI system prompt, error messages, `app/layout.tsx`'s page `<title>`, `package.json`'s `name`, dev-seed credentials, the data-export filename, and the local Supabase email templates. Two real bugs surfaced and got fixed along the way: the live confirmation email's *sender display name* and the *email templates themselves* are separate, dashboard-only Supabase settings that a code push can't reach (see Auth email confirmation below) — both needed manual updates in the Supabase dashboard, not just the code fix; and link previews (iMessage, SMS, etc.) were showing the default Next.js/Vercel favicon instead of the Areta mark because `app/favicon.ico` was still the unmodified starter icon and there was no real Open Graph image — fixed by deleting the stale favicon and adding `app/opengraph-image.tsx` + `app/apple-icon.tsx` (Next's dynamic-icon file convention, which generates the `<link rel="apple-touch-icon">` tag) plus `metadataBase` on the root metadata export, without which Next.js would have baked the `og:image` URL as `localhost:3000` even in production. The `apple-icon.tsx` route alone wasn't enough, though: iOS's Share-sheet "Add to Home Screen" doesn't reliably read that `<link>` tag — it directly probes the classic `/apple-touch-icon.png` path at the root, bypassing the HTML head, and 404s there fall back to iOS's own auto-generated first-letter icon. Fixed by also committing static `public/apple-touch-icon.png` / `-precomposed.png` files (rendered once from the same artwork via `next/og`'s `ImageResponse`, run standalone with `tsx`) at that literal conventional path. Not done: `CLAUDE.md` still says LifeOS throughout (by original design — see top of this file); the **GitHub repo** is still named `lifeos`; the old `getlifeos.tech`/`www.getlifeos.tech` domains are still live (not canceled, no redirect to the new domain set up) — see Known gaps.

**Calendar integration (Google + Outlook + Apple, read-only): done, verified end-to-end locally with three real accounts** (a Google account, a fresh Microsoft account's Outlook calendar, and iCloud Calendar via CalDAV) — a test event on each provider correctly appeared in the dashboard's "Upcoming events" section. `/settings/calendar` connects Google Calendar and Microsoft Graph via OAuth (`platform/calendar/google-provider.ts`, `microsoft-provider.ts`), and Apple/iCloud via CalDAV (`apple-provider.ts`, using `tsdav` + `ical.js` — Apple has no OAuth API, so it authenticates with an Apple ID + a user-generated app-specific password instead of a consent screen). All three normalize into one `NormalizedEvent[]` shape (`platform/calendar/provider.ts`); OAuth tokens and the Apple app-specific password are encrypted at rest (AES-256-GCM, `platform/calendar/token-crypto.ts`) rather than relying on RLS alone, since they're standing bearer credentials to a third-party account. The dashboard's "Upcoming events" section (`app/(app)/dashboard/data.ts`) shows real events, wrapped in a 3-second timeout with a graceful empty-list fallback so a slow or broken calendar connection can't hold up page load. A deterministic (no AI) free/busy engine (`domains/calendar/freebusy.ts`) computes open time windows for the next 7 days against the user's onboarding wake/bed time — surfaced for now as raw open blocks, not yet bound to a specific goal/task. Migration `0012_calendar_integration.sql` (the `calendar_connections` table) is pushed to production. Microsoft's OAuth request explicitly sets `prompt=select_account` so it always shows an account picker rather than silently SSO-ing into whichever Microsoft account is already active in the browser. Real-world setup notes from getting this working: Google Cloud OAuth clients can land in the wrong project if the project switcher isn't checked first; a fresh personal Microsoft account has no Entra ID directory of its own (needs either the M365 Developer Program — which can be declined eligibility — or a real Azure subscription signup to provision one); and Azure auto-adds a `User.Read` permission to every new app registration that has to be manually removed to keep the consent/approval screen scoped to exactly what's used (`Calendars.Read` + `offline_access`) — this matters for org/work accounts, where an admin-approval screen shows the app's full registered permission set rather than just what's requested per sign-in. **Not done**: production Vercel env vars (`GOOGLE_CALENDAR_CLIENT_ID/SECRET`, `MICROSOFT_CALENDAR_CLIENT_ID/SECRET`, `CALENDAR_TOKEN_ENCRYPTION_KEY`) are still unset, so nothing calendar-related works in production yet, only locally; the Google OAuth consent screen is still in "Testing" publishing status (fine solo, but needs flipping to "In production" — no verification review required at this scope — before any other user could connect); a recurring Apple event (RRULE) only returns its master occurrence, recurrence expansion isn't implemented; and a work/school Microsoft tenant with an admin-approval policy (confirmed against a real org account) blocks self-service connection until an org admin approves the app — expected behavior, not a bug.

**Wearables (CLAUDE.md §14): working end-to-end for weight, sleep, steps, heart rate, and workouts, verified on a real device.** The native HealthKit companion app (`areta-mobile`, standalone repo) reads all five types and syncs them into this app's Supabase project via `app/api/health-sync/route.ts`, a bearer-token-authenticated batch ingestion endpoint (auth via a Supabase access-token Bearer header rather than the cookie-based session the rest of the app uses, since a native client has no cookies to send — RLS still applies, no service-role bypass, just under the caller's own token). Every type shares the same provenance shape (`device`, `imported_at`, `user_override`, `dedup_key`) across `weight_logs`/`sleep_logs` (migration `0013_health_import_provenance.sql`) and the newer `step_logs`/`heart_rate_logs`/`workout_logs` (migrations `0015`/`0016`) — each insert path (`domains/{weight,sleep,steps,heartrate,workout}/service.ts`) upserts on `(user_id, dedup_key)` so a retried sync can't double-insert, and skips overwriting any row the user already hand-corrected. A basic read-only view lives at Settings → Health Data (workouts only, last 30 days, for now — weight/sleep/steps/heart-rate views are a natural follow-up, not yet built). Background delivery is now done on the mobile side too — `areta-mobile` registers native `HKObserverQuery` observers that survive app termination, so sync no longer requires a manual button tap; see that repo's own README for the config-plugin/anchor-reset details. **Not done**: this repo's `/api/health-sync` changes for steps/heart-rate/workouts are only verified against a local dev server, not yet deployed to production (the database migrations ARE live in production, ahead of the deployed route code); a conflict/correction UI for `user_override` (the column and skip-logic exist, nothing sets it from the UI yet).

**HealthKit-import retention window (3 years) + silver-layer daily analytics table: done, applied to production.** The mobile app's background sync had an unbounded-history bug (HealthKit's anchored query with no prior anchor returns oldest-first, not recent-first, so a fresh backfill got stuck deep in old history) — fixed by bounding automated HealthKit imports to a rolling 3-year window, enforced in **both** places: client-side in `areta-mobile`'s fetch functions, and server-side here via `platform/health/retention.ts`'s `isOlderThanHealthImportRetentionWindow`, checked at the top of each `insertImported*Log` function (skips, doesn't fail, matching the existing `user_override`-skip shape — `summarize()` and the mobile UI need no changes to reflect it). Manual-entry paths (`logWeight`, `logSleep`) are untouched; retention only applies to the automated import path. Migration `0017` did a one-time, product-wide purge of existing out-of-window rows (already applied). On top of that, a new `activity_daily_summaries` table (migrations `0018`/`0019`) is a silver-layer rollup — one row per user per day, populated incrementally by a `domains/activity-summary/service.ts` recompute function called from every log-insert path (not a cron job — this repo has none, matching the existing `weekly_reviews`/`weekly_outcomes` convention of on-demand computation) — tracking workout count/minutes/first-and-last start time/distinct activity types (deduped, chronological order), weight first/last value of the day, steps total + most-active local hour, sleep duration/quality, and heart-rate average, plus `day_of_week`/`is_weekend` as Postgres generated columns. Correctly timezone-aware via `profiles.time_zone` (falling back to UTC only if unset) — unlike `todayDateString()`'s known UTC-simplification gap elsewhere in this app (see Known gaps), getting this right matters here since the whole point is *when* something happened; DST-transition correctness is covered by real unit tests (`tests/unit/activity-summary-timezone.test.ts`). A one-time backfill script (`scripts/backfill-activity-daily-summaries.ts`) populated all existing history (116 rows across active accounts as of this writing) by reusing the exact same recompute path incremental writes use. This is explicitly a foundation for future work, not the finished feature: a monthly rollup, pattern-detection queries (e.g. "which day/hour do they usually work out"), and any reminder/notification delivery are all deferred — no scheduler or push infrastructure exists yet.

**HealthKit expanded to the full fitness/vitals surface (23 types total), storage consolidated into one table: done, applied to production, verified via typecheck/tests.** The mobile app now reads 18 additional HealthKit types beyond the original five — VO2 max, resting heart rate, heart rate variability (SDNN), walking heart rate average, active/basal energy burned, distance walking-running/cycling, body fat %, lean body mass, BMI, height, flights climbed, walking speed/steadiness, oxygen saturation, respiratory rate, and mindful minutes (see `areta-mobile`'s `lib/healthkit.ts`, `METRIC_REGISTRY`). Rather than 18 more near-identical `*_logs` tables, all 23 types (the 18 new ones plus the original weight/sleep/steps/heart_rate/workout) were consolidated into one `health_metrics` table (`metric_type` discriminator column), migrated from the five legacy tables via `INSERT ... SELECT` and verified by an exact row-count match (weight 10, sleep 9, steps 1,206, heart_rate 1,260, workout 243 — 2,728 rows) before the legacy tables were **renamed, not dropped** (`weight_logs_deprecated_migrated` etc., plus untouched `*_backup_pre_health_metrics` snapshot copies) as a safety net, since no Supabase branch was available to test the migration in isolation first (branching requires a Pro-tier plan this project doesn't have). A real data-fidelity bug surfaced and got fixed during migration: `sleep_logs.total_duration_minutes` was an independently-tracked field, not always equal to `wake_time - bedtime` (a user can be in bed longer than they're actually asleep) — naively deriving duration from the new `started_at`/`ended_at` interval would have silently dropped that distinction for one real row, so sleep duration is now stored explicitly in `health_metrics.value` instead of derived. `platform/health/metrics.ts` holds the shared insert/retention/dedup logic (`insertManualHealthMetric`/`insertImportedHealthMetric`) every type's domain wrapper now calls, replacing what used to be duplicated near-identically five times; `/api/health-sync/route.ts` dispatches through a per-metric-type handler registry instead of hand-writing a `Promise.all` block per type. The wire contract for the three simple import-only types that predate this pass (weight/steps/heart-rate) was also unified to the same `{value, unit}` shape the 17 new simple vitals types use (previously `weight`/`count`/`bpm` respectively), so the mobile client can build one generic envelope for all 20 point-in-time types rather than a bespoke shape per type. Every consumer of the five legacy tables was found and updated (`domains/{weight,sleep,steps,heartrate,workout,activity-summary,review,prompts,parameters,account,trainer}/*`, `app/(app)/dashboard/data.ts`, `app/api/exercise/route.ts`, `scripts/backfill-activity-daily-summaries.ts`) — a full-repo grep turned up more call sites than the initial pass found (weekly review, prompt triggers, nutrition-target calculation, account data export, trainer client views), all confirmed non-regressed via `pnpm typecheck` + `pnpm test` (318 tests passing) after the rewrite. The 18 new vitals types are import-only (no manual-entry form, matching steps/heart-rate/workouts' existing pattern) and deliberately don't roll up into `activity_daily_summaries` this pass — stored for future features to build on, not surfaced anywhere yet. **Not done**: no UI surfaces any of the 18 new types; the legacy renamed/backup tables are still sitting in the database, not yet dropped (pending confidence the new pipeline is solid); verification was typecheck/tests only, not yet exercised against real device Health data (the Simulator has no real vitals data to seed).

**On-demand training-content pipeline: done, verified against the live Supabase project.** The Exercise tab's `training_programs` system (9 archetypes, 27 programs, hand-authored as raw SQL migrations `0032`-`0040`) now has a real content-authoring pipeline instead of hand-typed SQL — `domains/trainingprogram/content-spec.ts` defines a typed `ContentBatch` format (a Zod discriminated union on `kind: "strength" | "cardio"` per exercise prescription, which structurally prevents the exact column-arity bug that hand-authoring caused originally), validated by `scripts/training-content/validate-spec.ts` (slug uniqueness, exercise-reference resolution, phase/session shape) and gated by `scripts/training-content/verify-sources.ts` — an automated credibility check (not human review, per explicit product decision) requiring every program's citation to match `domains/trainingprogram/source-allowlist.ts` (certifying bodies, national governing bodies, peer-reviewed journals, and named specialists like Jeff Cavaliere/Andy Galpin/Gunnar Peterson with domains verified via live search, not guessed) and to actually resolve via a live fetch. `scripts/training-content/generate-migration.ts` turns a validated batch into a real migration file (one INSERT per exercise-prescription row, never a batched multi-row VALUES list, which is what makes the arity bug structurally impossible now). New `program_sources` table (migration `0041`) stores the citation trail per program. Purely additive by design — this pipeline never deactivates or modifies an existing program. Run via `pnpm run content:add -- --spec <path>`; full runbook (including the "who's the right specialist for this archetype" research step) at [`docs/training-content-pipeline.md`](./docs/training-content-pipeline.md). Deliberately on-demand only for now, not a scheduled/autonomous service.

**Exercise alternatives (swap feature): done, deployed, and verified against the live Supabase project and the iOS simulator.** Today's prescribed exercise (e.g. "Rowing intervals") can now show up to 2 curated, same-training-purpose alternatives (e.g. Assault bike sprints, Treadmill intervals) the user can swap to for the day — `program_session_exercises.primary_exercise_id` (migration `0042`) is a nullable self-referencing FK: null on the recommended/default row, set to that row's id on each alternate. `domains/trainingprogram/service.ts`'s `getSlotOptions()` resolves "every other option for this slot" for a batch of current selections in one query, correctly including the slot's own recommended default when the current selection is itself an alternate (otherwise a user who'd already swapped away from the default would have no way back). `swapWorkoutPlanItemExercise` (`domains/workoutplan/service.ts`) validates server-side that a requested swap target is an actual sibling of the item's current prescription before applying it. Alternates were retrofitted onto cardio-flexible slots across most of the 9 archetype programs (migrations `0043`, `0045`) rather than waiting for future content passes. A content-quality audit (migrations `0046`-`0048`) found and fixed a real display bug affecting several interval-structured cardio rows (both retrofitted alternates and a few original rows): `duration_minutes` holds the *per-interval* length for interval work (e.g. "3" in "6 x 3 min hard, 2 min easy"), not the total session length, so `formatPrescription()` (mobile) was misreading it as "3 min" for the whole workout — fixed by showing `cardio_intensity`'s own text directly whenever `sets > 1` signals an interval/rounds structure, only using the "X min (...)" prefix for genuinely continuous cardio.

**Legacy generator truncation fix + a 4th hybrid_athlete program: done, pushed to production.** Investigating a user report of 1-2-exercise workout days surfaced two distinct issues: (1) a real bug in the legacy archetype-only `generateWorkoutPlan` (`domains/workoutplan/generate.ts`) — its slot-fill loop silently `break`s when the eligible exercise pool runs dry before reaching the target session length (e.g. a narrow equipment/archetype match), producing an unexplained short session; fixed by cycling back through the eligible pool to always reach the target length, with a warning when the pool is narrower than requested (regression-tested in `tests/unit/workout-plan-generate.test.ts`). (2) Separately, the existing hybrid_athlete/functional_fitness/olympic_weightlifter `training_programs` content is genuinely authored with 1-2 exercises per session throughout — not a bug, but thinner than hypertrophy/powerlifter/general_fitness's 3-4/day. Rather than hand-editing that existing content (against this pipeline's purely-additive rule), a new 4th hybrid_athlete program, **Full-Coverage Concurrent Training** (migration `0049`, slug `hybrid-full-coverage-concurrent`), was authored through the real content pipeline: strength-focused sessions now get full major-movement-pattern coverage (squat/hinge/horizontal push/pull/core, 4 exercises), while conditioning-priority and aerobic days deliberately stay single-modality, per real sourced guidance (Alex Viada/Complete Human Performance — the specialist most associated with the term "hybrid athlete" — on consolidating stressors so no session overloads one energy system, plus a PMC systematic review on concurrent-training exercise sequencing). `completehumanperformance.com` added to `source-allowlist.ts`. The existing thinner hybrid_athlete/functional_fitness/olympic_weightlifter programs are untouched and still in rotation (purely additive) — functional_fitness and olympic_weightlifter equivalents are a natural follow-up, not yet done. Also surfaced along the way: the mobile Exercise tab now shows today's session name (e.g. "Heavy Strength") even on a short day, so an intentionally light session reads as designed rather than broken; a weekly cron (`app/api/cron/regenerate-workout-plans`) keeps anyone whose active plan has gone stale generated & auto-activated a `SELF_SERVICE_WEEKS_AHEAD`-week rolling window ahead (a later, 2026-08-09 policy change from this cron's original never-auto-activates behavior — see that route's own doc comment). `CRON_SECRET` was set in Vercel production 2026-08-12 and verified working (manually invoked; regenerated stale plans for real users) — it had been unset since this cron shipped, so it was never actually running before that.

**Widening the thin-content fix beyond hybrid_athlete: in progress.** A closer audit (prompted by the same 1-2-exercise-workout complaint recurring against a live test account still on the old thin `hybrid-daily-undulating` program) found the 1-2-exercise-per-session pattern wasn't limited to the three archetypes named above — it's present in **145 of 366 sessions across 23 of the system's original 27 programs** (`0032`-`0040`), every archetype except triathlete. Given that scale, and that this pipeline's purely-additive rule means new good programs never retroactively help users already on a thin one, the approach changed: migration `0053` sets `is_active = false` on all 23 thin programs (data preserved, not deleted or edited — `is_active` only removes a program from future `getEligibleProgramCandidates` selection; already-assigned users are unaffected until their own natural reselection point). This is an explicit, deliberate product decision to prioritize new users getting good content over strict non-interference with old content, made after confirming with the user that endurance archetypes' (`long_distance_runner`/`cyclist`) "Strength Support" accessory days get no exception — same ≥4-exercises-per-strength-session bar applies everywhere. 6 of 8 affected archetypes (`powerlifter`, `hypertrophy`, `long_distance_runner`, `cyclist`, `olympic_weightlifter`, `functional_fitness`) had *zero* active programs immediately after `0053`; `general_fitness` kept `general-fitness-circuit` (not a strength-session-count question), `hybrid_athlete` kept `hybrid-full-coverage-concurrent`. Replacements are being authored one archetype at a time through the real content pipeline: **powerlifter done** — `powerlifter-conjugate-full-coverage` (migration `0054`, slug `powerlifter-conjugate-full-coverage`), grounded in Westside Barbell/Louie Simmons' actual published Conjugate Method (Max Effort + Dynamic Effort days, each paired with real repeated-effort accessory volume per the source's own ~80%-of-volume-is-accessories framing) — `westside-barbell.com` added to `source-allowlist.ts`. **hypertrophy done** — `hypertrophy-athleanx-ppl-full-coverage` (migration `0055`), reusing the already-allowlisted Jeff Cavaliere/ATHLEAN-X entry (already the best-matched specialist for this archetype, per the pipeline doc's own example) and grounded in ATHLEAN-X's actual published Push/Pull/Legs guide, which runs each of Push/Pull/Legs twice weekly with 6-7 exercises per day — this program uses 5 per session across all 6 weekly sessions. **long_distance_runner done** — `runner-daniels-five-pace-full-coverage` (migration `0056`), grounded in Dr. Jack Daniels' (Daniels' Running Formula / VDOT) actual five-training-pace framework for the running sessions themselves (Easy/Threshold/Interval/Repetition, each single-modality by design, same as any cardio-type session elsewhere) plus a real 5-exercise Strength Support day (previously 1-3) -- `vdoto2.com` added to `source-allowlist.ts`, two new exercises added (`Track interval repeats`, `Track speed repetitions`) since the library had no non-machine interval/repetition running movements. This surfaced and fixed a real bug in the content pipeline itself: `scripts/training-content/generate-migration.ts`'s `emitNewExercises` predated migration `0044`'s `canonical_name`/`movement_patterns` NOT NULL columns and didn't emit them, so any batch adding new exercises would fail at apply time -- fixed to derive both from `name`/`movement_pattern` the same way `0050_expand_exercise_library_variety.sql` already does by hand. **cyclist done** — `cyclist-coggan-power-zones-full-coverage` (migration `0057`), grounded in Andrew Coggan's 7-zone power-based training system (documented by his co-author Hunter Allen) for the ride sessions (Endurance/Tempo/Threshold/VO2max/Sprint, single-modality by design, mapped directly onto the app's existing cycling cardio exercises) plus a real 5-exercise Strength Support day -- `hunterallenpowerblog.com` added to `source-allowlist.ts`. **olympic_weightlifter done** — `oly-catalyst-full-coverage` (migration `0058`), grounded in Greg Everett/Catalyst Athletics' publicly documented programming structure (classic lifts + dedicated pulling/squat strength + accessory work per his own program-selection guide) across 4 sessions (Snatch, Clean & Jerk, Squat & Strength, Pulling & Technique), 4-5 exercises each -- `catalystathletics.com` added to `source-allowlist.ts`. **functional_fitness done** — `functional-fitness-comptrain-full-coverage` (migration `0059`), grounded in Ben Bergeron/CompTrain's unified strength/conditioning/mobility programming (per comptrain.com), across 3 strength-type days (Strength, Gymnastics Skills, Olympic Lifting, 5 exercises each) plus a Conditioning day built as a genuine 5-movement for-time chipper rather than a single exercise standing in for the whole session -- `comptrain.com` added to `source-allowlist.ts`.

**All 8 affected archetypes now have a full-coverage active program.** Every archetype has at least one active program with ≥4 exercises per non-exempt strength session: `powerlifter` (`0054`), `hypertrophy` (`0055`), `long_distance_runner` (`0056`), `cyclist` (`0057`), `olympic_weightlifter` (`0058`), `functional_fitness` (`0059`), plus the pre-existing `hybrid-full-coverage-concurrent` (`0049`) and `general-fitness-circuit`/triathlete's original 3 programs, which were never thin. New users in every archetype now get real, sourced, full-coverage content. What this does **not** do: retroactively move any already-mid-program user (including the test account, still on the deactivated `hybrid-daily-undulating` until it naturally reaches a reselection point) onto a new program -- that's a separate, deliberate action, not a side effect of adding/deactivating programs.

**Per-archetype exercise counts raised past the ≥4 floor: done (migration `0067`).** A follow-up user report found every full-coverage session above was authored to satisfy "at least 4" literally, never higher -- every archetype sat at exactly 4 or 5, never more. Rather than one blanket target, researched actual per-archetype norms across multiple specialists each (not just the one program's own cited source, so no single rigid philosophy drove the number) via WebSearch/WebFetch: **hypertrophy** 5→7 (ATHLEAN-X's own real Push/Pull/Legs routine runs 6-7; additions are pulled directly from that real routine, not invented). **long_distance_runner** 5→8 (TrainingPeaks' explicit "6-8 exercises" for runner strength-support work -- the only hard number found). **general_fitness** (circuit) 3→6 (general web consensus for intermediate lifters). **hybrid_athlete** 4→5 -- the one archetype where the cited specialist (Alex Viada) explicitly argues for *fewer* exercises, not more ("skip the fourth accessory"); rather than overriding him or blindly deferring to him, found two more hybrid-specific sources with real sample sessions (BOXROX, Tailored Coaching Method, both added to `source-allowlist.ts`) and landed on their shared mode of 5. **cyclist** 5→6 (Brodie's explicit 4-6 range). **functional_fitness** 5→6 (modest increase -- CompTrain/Invictus publish no hard per-session number, flagged as lower-confidence). **powerlifter** and **olympic_weightlifter** deliberately **unchanged** at 5 -- checking Sheiko and Wendler 5/3/1 alongside Westside showed real powerlifting methodology clustering at 2-5, and Catalyst Athletics' own template plus the Chinese system cluster at 4-5, both already at/above that ceiling; raising them further would have contradicted every specialist checked, not just one. All additions are either exercises pulled directly from a cited specialist's real published routine, or reused from the exercise library already tagged for that archetype -- hand-written migration (not routed through `content:add`), same precedent as `0045`, since this is structural data on already-shipped, already-cited programs rather than a new methodology claim. `trainingpeaks.com`, `boxrox.com`, `tailoredcoachingmethod.com` added to `source-allowlist.ts`.

**Workout rationale ("why this workout"): done, on the mobile Exercise tab.** `domains/workoutplan/rationale.ts`'s `buildWorkoutRationale` -- pure, rule-based, unit-tested (`tests/unit/workout-plan-rationale.test.ts`) -- explains today's session against the user's own real *adjacent-day* schedule (today's/tomorrow's actual materialized session type + real exercise count from `workout_plan_items`, not a description of today in isolation): a lighter strength day ahead of tomorrow's long run, a heavier day ahead of a lighter one, etc. Deliberately does **not** fall back to `training_program_phases.focus` (a phase-level description, not an explanation of this specific workout) -- returns null rather than reaching for that when no specific today/tomorrow relationship applies. That phase-focus data is untouched in the DB/plan payload; it's earmarked for a future Plan/Review screen (both still placeholder stubs as of this writing) rather than this per-day feature. Wired into `GET /api/exercise`'s new `rationale` field, replacing the mobile Exercise tab's old hardcoded "short session" note (which only fired below 3 exercises and said nothing about *why*).

**Workout swaps now rebalance the rest of the week, not just today.** Found via a user report of back-to-back "Heavy Strength" days: `materializeWorkoutPlan` only ever uses as many of a phase's authored sessions as there are training days that week (e.g. 5 of a hybrid_athlete program's 6 sessions for a 5-day week), so `selectAlternativeSessionForToday` overwriting only today's `day_of_week` could silently duplicate an already-scheduled session and drop whatever that day was going to be -- exactly what happened when a swap collided with a session already materialized on another day. Now a swap is a pairwise exchange: if the target session is already scheduled today-or-later this week, that day trades places with today (today's old session moves there, no duplicate, nothing dropped); if the target is the "leftover" session that week's materialization didn't use, today just takes it directly. A day that's already passed is never touched -- explicit product decision, since a completed/logged day shouldn't visually change after the fact; if a target session's only occurrence this week is on a past day, it's treated the same as the leftover case rather than rewriting history. Verified against live data in an isolated worktree before merging (both the leftover and swap-partner branches, in both trade directions) -- no mobile or API-contract changes needed, since the client already re-fetches and re-renders whatever the DB reflects.

**Goal-first personalized training system: Phases 0-4 done and deployed — schema, onboarding (web + mobile), content (evidence layer, limitation rules, exercise enrichment, a 252-template generated matrix), and the recommendation engine itself (`domains/recommendation/*`) are all live; weekly adaptation auto-regulation (the check-in UI + volume adjustment loop) and scheduled expert discovery are the remaining phases.** Replaces the archetype-first `training_programs`/rotation system above with a goal-first architecture: user needs determine program structure first, then expert-cited evidence informs which exercises fill it — never assigning a whole trainer's methodology just because of a goal match. Migration `0044` adds the full schema: `experts`/`expert_claims`/`sources` (atomic, source-cited claims replacing a vague `endorsedBy` field), `program_templates`/`template_phases`/`template_sessions`/`template_slots` (the new model's leaf table names an abstract movement slot, not a concrete exercise — exercises get chosen at weekly-generation time once the recommendation engine exists), `workout_plan_item_alternatives` (re-anchors the swap-alternatives mechanism above to the materialization layer, since slots are filled dynamically per user per week rather than authored once per program), `limitation_rules` (explicit exclude/substitute/manual-review enforcement per limitation tag — `action='exclude'` is a hard filter applied before scoring, never a scoring penalty), `weekly_adaptation_responses`, and `profiles.is_admin` (the first admin-vs-everyone RLS split in this schema). The Exercise onboarding step is rewritten to the brief's 7(+1)-question goal-first shape (`domains/exercise/schema.ts`) — the old archetype-first shape is preserved in `domains/exercise/legacy.ts` purely so already-onboarded users can be detected (`isLegacyExerciseShape`) and prompted to redo onboarding once; the still-live archetype/rotation pipeline gracefully returns "coming soon" for anyone re-onboarded in the new shape until the actual recommendation engine (Phase 4) replaces it, rather than crashing or generating a broken plan. New bearer-authenticated `app/api/onboarding/*` routes (mirroring the swap feature's own `authenticateBearerRequest` pattern) let mobile drive the exact same onboarding domain layer as web — every `save<Domain>Step` function and `domains/onboarding/{store,orchestrator}.ts` now accept an optional injectable Supabase client, the same convention used everywhere else in the domain layer. Per an explicit "nothing in this app should be web-only" decision, **all six onboarding steps got a mobile-native build in the same pass**, not just Exercise — see `areta-mobile`'s own README/commit history for that side.

**The recommendation engine (2026-08-07) closed the "coming soon" gap.** `domains/recommendation/*` is the deterministic engine 0044's schema anticipated: `select-template.ts` (nearest-match template selection over a fully generated goal x tier x equipment-context x duration-band matrix — 252 templates emitted from 7 authored blueprints by `scripts/training-content/generate-templates.ts`, methodology per goal: RP volume landmarks, ACSM progression models, Seiler 80/20 + Daniels long-run structure, FITT-VP), `fill-slots.ts` (abstract movement-pattern slots -> concrete exercises: hard safety floor from `limitation_rules` + per-exercise limitation tags that is never relaxed, substitute-rule pattern re-targeting, progressive relaxation with warnings, preference/compound/difficulty/variety scoring, top-2 alternates persisted to `workout_plan_item_alternatives`, per-item provenance with expert-claim ids), `prescribe.ts` (RP-style set ramp with final-week deloads), and `progression.ts` (continue/advance/reselect phase transitions). `generateAndSaveWorkoutPlan` and `generateExerciseParameters` route new-shape users through it while legacy archetype users keep the old pipeline byte-for-byte; the evidence layer (`experts`/`sources`/`expert_claims`) is seeded with ACSM/NSCA-grounded plus named-practitioner (Israetel, Galpin, Cavaliere, Schoenfeld, Seiler, Daniels) claims so every generated item carries citable provenance. Verified end-to-end against production by `scripts/verify-goalfirst-engine.ts` (three goal scenarios + a lower-back-limitation scenario + a legacy-path regression, all on throwaway fixture users).

**Admin portal + trainer role foundation: done, deployed.** (Note: this shipped via an earlier merge and was never separately documented here until now.) `/admin` — owner/reviewer-gated (`profiles.is_admin`/`admin_role`, migration `0064` audit log) — covers a merged Evidence tab (experts/sources/claims/limitation-rules, previously four disconnected tabs), exercise/recipe content management, a users tab with search/role-change/delete, and an owner-only Ops tab (audit log, AI-run history, cron status). `profiles` write access is column-scoped (migration `0065`) after finding Supabase's default grants let a client write `is_admin` directly via PostgREST, bypassing app code entirely. Separately, a **trainer role** (`profiles.is_trainer`, `trainer_clients`, `trainer_invite_codes` — migrations `0066`/`0068`) lets an owner-granted trainer view/edit a linked client's goals and nutrition/workout plans via RLS policies additive to the existing owner-only ones; clients redeem an invite code at `/settings/trainer`. This is the B2B2C foundation the feature below builds on.

**Trainer-authored, reusable multi-week training programs: done, deployed, verified against the live Supabase project and in the browser as both a trainer and a client seed account.** Trainers get their own program library (`trainer_programs` → `trainer_program_phases` → `trainer_program_sessions` → `trainer_program_session_exercises`, migration `0075`) — phases with a length in weeks and (originally) an explicit repeat-or-freeze-on-completion rule, later removed entirely (see the pass documented below), sessions per day-of-week, exercises with sets/reps ("reps", not min/max — simplified after feedback that the split added no value) and optional intensity/cardio/coaching-note fields. A trainer builds a program once and assigns it to any number of clients (`trainer_program_assignments`), each with its own start date, a required hard-cutoff **end date**, and a required linked client-facing **goal** (a real row in the client's own `goals` table, not just descriptive text — created and rolled back transactionally with the assignment). The whole thing is driven by one pure projection function, `projectProgramRange` (`domains/trainerprogram/calendar-projection.ts`, fully unit-tested) — given a program's phases, an assignment's start/end dates, and any per-date overrides, it deterministically resolves every calendar date to a phase/week/session with zero stored "current position" pointer to drift out of sync. Both the trainer's month-by-month calendar view (drag-to-move a session, per-date overrides including marking a day as rest, a search-plus-add-new exercise picker for on-the-fly customization) and the real weekly generator (`domains/trainerprogram/materialize.ts`) call this same function, so what a trainer sees on the calendar is guaranteed to match what actually lands in the client's plan. Past the end date, an assignment auto-ends (status `ended`, never deleted) and surfaces in a "past programs" list a trainer can reassign later, with modifications. RLS additionally scopes a trainer's goal/domain access to only the client's **active, fitness-relevant** goals (nutrition/exercise/recovery) — never past/completed goals or personal (career/finance) ones (migrations `0077`/`0079`). The old "generate from the shared library" option was **removed entirely** for trainers — an explicit product decision that a client paying a trainer shouldn't get the same auto-generated plan the free app already produces; a trainer must actually customize a program to assign one.

Two friction points surfaced through real use both got explicit, opt-in fixes, each a deliberate exception to CLAUDE.md's "require approval before changing active plans" rule: a per-assignment **auto-approve** toggle (migration `0080`, since removed — see below) skipped the weekly manual-approve click entirely, and a manual **"push weeks live"** action (`generateAndApproveWeeksThrough`) lets a trainer who's customized several weeks ahead on the calendar activate all of them through a chosen date in one click (capped at 12 weeks and at the assignment's end date) instead of waiting for the weekly cron to reach each one or re-approving one at a time — this action is still live. Building the second of these surfaced and fixed a real pre-existing bug: `getWorkoutPlanForWeek`/`approveWorkoutPlan` matched `workout_plans` by an exact `week_start = today` comparison, which only ever aligned on the literal day a plan was generated or approved — any other day, the dashboard and `/plan/workouts` showed "no workout plan" despite an active plan existing. Fixed with `getCurrentWorkoutPlan` (exact match first, falling back to the most-recent active plan) now wired into the dashboard, `/plan/workouts`, and the trainer's client-overview page. **Nutrition has the identical latent bug** (`/plan/meals` still uses the exact-match lookup; the resilient `getActiveMealPlan` already exists but isn't wired in) — flagged, not yet fixed, deliberately deferred to keep this pass workout-only.

**Trainer workout UX pass — exercise picker unification, program-lifecycle fixes, plain-language copy: done, deployed, verified against the live Supabase project and in the browser as the dev trainer/client seed accounts.** Supersedes two behaviors described in the trainer-authored-programs entry above, both removed after direct user feedback: the phase-level repeat/freeze-on-completion rule (`trainer_program_assignments.on_complete`) and the per-assignment auto-approve toggle (`auto_approve`) are both gone (migrations `0081`/`0082`) — every trainer-program materialization now writes straight to `active`, no draft/approval step, since the founder's explicit read of CLAUDE.md rule 10 is that it protects a user's own self-service plan generation, not a trainer acting on a client who already consented by accepting them (the item-level customize/add-exercise trainer actions no longer demote an active plan back to draft either, for the same reason). A program's phases running out before its end date no longer auto-continues in either direction — a new `projectProgramRange` "phases_complete" state (distinct from not-started/ended) shows on the calendar and day list, and the trainer has to manually assign a new program or fill the gap with calendar overrides.

The categorized (browse-by-muscle-group) exercise picker from the mobile Exercise tab was ported to web and unified across all four trainer-facing exercise pickers — the program builder, the calendar day editor, and (previously still a plain 100+ option `<select>`) the client workout page's "Customize"/"Add exercise" actions, via a new `ExerciseSearchField` split out of the existing `ExercisePicker`. A trainer's own not-yet-reviewed custom exercises now show up in all four (the workout page specifically was on `getAllExercises()`, the client-app-scoped active-only fetch). Cardio exercises now group into one "cardio" bucket regardless of which muscle group is listed first in the data (most are tagged e.g. `["legs", "cardio"]`, so the previous first-tag-only grouping scattered them across "legs"/"full body"/"shoulders"). Found and fixed a real interaction bug along the way: selecting a category could synchronously unmount the clicked button before the outside-click listener's bubble-phase handler ran, misreading its own click as "outside" and closing the dropdown — switched the check from `Node.contains()` to `Event.composedPath()`, captured at dispatch time before any such re-render.

Fixed a real data bug found live: switching or removing a client's assigned trainer program left that program's already-materialized `workout_plans` rows dangling as `active`, so a stale day's content could keep showing indefinitely if the new program hadn't started generating yet (most visibly when the new program's start date is still in the future). `getWorkoutPlanForWeek`'s exact-date lookup has no idea which program a row belongs to, only its date. New `archiveStaleTrainerProgramPlans` marks the old program's today-or-future rows `archived` (a `workout_plans` status the schema allowed since migration `0010` but nothing had ever set) right after a switch/unassign commits; `getWorkoutPlanForWeek` now excludes archived rows.

Smaller fixes and polish in the same pass: "push weeks live" now defaults its date to the assignment's own end date instead of a flat +28-day guess; the workout page's per-day exercise lists collapse behind a `<summary>` by default (exercise count visible, one click to expand) instead of always-expanded cards; the ambiguous "Active" pill that sat above a single week's day list (misleading on a multi-phase program, since it never showed more than one week at a time) is now "Current week split," with a real Active/Upcoming status pill moved to the assigned-program card where it's actually meaningful; every trainer-facing tooltip and description was swept for jargon ("cron," "materializes," "phase cycle") and length, rewritten short and plain throughout; the trainer dashboard's page title changed from "Your clients" (now just a section heading) to "Dashboard," with short descriptions added under every subsection (invite codes, incoming requests, clients).

**Trainer-authored, reusable nutrition programs: done, deployed, verified against the live Supabase project and in the browser as the dev trainer/client seed accounts.** The nutrition-side counterpart to the trainer-authored workout programs above, replacing the old shared-algorithm meal-plan flow entirely for trainer-managed clients (`trainer_meal_programs` → `trainer_meal_program_phases` → `trainer_meal_program_meals`, migration `0083` — one structural simplification vs. the workout side: no session-grouping level, since a meal slot is already fully identified by `(dayOfWeek, mealType)`). A trainer builds a program from the shared recipe library or their own submitted recipes (`recipes.created_by`, reviewed like trainer-submitted exercises), assigns it to a client with a start/end date and a required linked goal (same pattern as workout assignments), and can recycle it across clients or phases. Portion size is deliberately **not** auto-scaled to a client's calorie target — per direct product feedback, a trainer manually tailors it per client, with the app only *recommending* a starting point (`domains/trainermealprogram/portion-recommendation.ts`: deterministic, calorie-target-divided-by-daily-meal-slots-divided-by-recipe-calories, rounded to the nearest quarter serving, clamped 0.25x-4x, unit-tested) that a trainer can accept or override per meal (`trainer_meal_program_portions`, one row per assignment+meal — never baked into the program itself, since the same program can serve clients with very different targets).

Two further passes closed real gaps found through use. First, **engagement-scoped nutrition targets** (migration `0084`): a client's `calorie_target`/`protein_target_g` are paced against *their own* long-range goal timeline, which often doesn't match a given trainer's actual engagement window (a client's goal might run a year; a trainer might only be hired for 13 weeks) — a trainer can now recompute a target scoped to just their own assignment's start/end dates, reusing the exact same deterministic Mifflin-St Jeor engine (`domains/parameters/nutrition-calc.ts`, factored into a shared `getNutritionCalculationBaseInputs` so this doesn't duplicate the client's own calculation path) with full rationale/assumptions/safety-bounds shown, same transparency bar as the client's own `/plan/parameters`. Verified live against a real mismatched-timeline case: a 4-week engagement against a client's real weight-loss goal computed a required 5.2 lb/week pace, which the engine correctly safety-capped at 2 lb/week and surfaced as a warning. Stored as an assignment-scoped override (`trainer_meal_program_assignments.nutrition_override`), never written into the client's own `generated_parameters` row — their long-range target keeps driving their own dashboard/self-service plan untouched, and a trainer can revert to it any time.

Second, **materialization into `meal_plans`/`meal_plan_items`** (`domains/trainermealprogram/materialize.ts`): an assigned program's meals now actually reach the client's real nutrition views, not just the trainer's portion-review screen — the same gap workout programs closed earlier via `domains/trainerprogram/materialize.ts`. One structural difference from the workout side: nutrition has no calendar/override system yet, so there's no per-date projection needed — a program meal's `day_of_week` is already a literal Sun(0)-Sat(6) slot (the same convention `meal_plan_items` and `getWeekDates` already use), so a phase's meals copy across directly. Every write path that changes what a client's plan *should* contain (assign, unassign, save portions, save/clear an engagement target, or a trainer editing the program's own meals/phases) re-materializes the client's current week automatically, and a weekly cron (`app/api/cron/regenerate-meal-plans`, Mondays 06:05 UTC) keeps it from going stale between edits. `CRON_SECRET` was set in Vercel production 2026-08-12 and verified working, same as its workout sibling. A client's own self-service "Generate meal plan" button now refuses to run while a trainer program is assigned (mirroring the equivalent workout-side guard, added at the same time this became a real write-conflict risk — both paths upsert onto the same `(user_id, week_start)` row). Switching or unassigning a program archives its stale materialized plans, same `archived`-status pattern used on the workout side.

Known gaps in what's built so far:
- The Playwright e2e spec (`tests/e2e/`) is written but requires a local Supabase stack via Docker (`supabase start`), which hasn't been available in this environment — it hasn't actually been run yet. Everything else was verified with unit tests plus manual browser testing instead.
- `confirmOnboarding`'s multi-table write (`domains/onboarding/write-output.ts`) is sequential inserts, not a single DB transaction — a partial failure mid-write can leave some tables written and others not. Fine for a single-user MVP; worth hardening with a Postgres function before this is multi-user.
- Password policy relies entirely on Supabase Auth's built-in minimum (8 characters) — no additional strength rule.
- Vercel deploys do **not** automatically run `supabase db push` — new migrations need to be pushed manually before/after a deploy that depends on them.
- Resend's free tier caps outgoing email at **30/hour**. Fine for early use; raise it from the Resend dashboard before any real launch push.
- `todayDateString()` (`app/(app)/dashboard/data.ts`) uses UTC, not the user's local timezone — the "today" a user sees around midnight local time may not match calendar-today for their timezone. This gap does NOT apply to `activity_daily_summaries` (see Wearables/retention status above), which properly resolves each user's local day via `profiles.time_zone` — worth fixing `todayDateString()` to match that approach at some point, rather than the other way around.
- `inventory_items` has a table and is wired into grocery-list subtraction, but there's no UI to edit it yet — every user's inventory starts (and stays) empty, so grocery lists never actually subtract anything today.
- The recipe library (24 recipes, seeded via migration) is small and fixed — no per-user recipes, no substitutions UI.
- The Weekly Operating Brief only covers the qualitative sections (summary, progress, priorities, changes, risks, motto, highest-leverage action). CLAUDE.md's full spec also calls for an AI-organized recovery plan, a learning plan, appointments, and a regenerated daily schedule — none of those exist yet.
- The Recommendation Feedback Loop (CLAUDE.md §8) only goes as far as accept/reject at approval time (`recommendations.accepted`). The `memories` table exists and feeds brief generation, but nothing yet cross-references memories against recommendation outcomes to close the "use successful strategies more often" loop.
- `weekly_reviews`/`recommendations`/`ai_runs` have RLS but no per-user rate limiting on `generateWeeklyBrief`. Fine for a single founder account; worth adding before multi-user.
- No account deletion — Settings > Account can export data and restart onboarding, but there's no self-serve way to delete the account itself.
- **Rebrand loose ends** (see "Areta rebrand" status above): the GitHub repo is renamed to `areta` (done); `getlifeos.tech` is still live with no redirect to `areta-ai.com` and hasn't been canceled; `CLAUDE.md` intentionally still says LifeOS throughout except §14, which was updated alongside the wearables work below.
- **Calendar integration** (see status above) is verified working locally against real Google, Microsoft, and Apple accounts, but production Vercel env vars are still unset, so nothing calendar-related works on areta-ai.com yet — only locally. Recommended-times are raw open windows, not yet bound to a specific goal/task.
- **Wearables** (see status above) works end-to-end for all five HealthKit types, verified on a real device — but only against a local dev server; this repo's route/domain changes for steps/heart-rate/workouts aren't deployed to production yet, even though the database migrations are already live there. Same caveat applies to the retention-window enforcement and `activity_daily_summaries` recompute wiring — verified locally and the migrations/backfill are live in production, but the route code itself needs a deploy.
- **Goal-first training system** — the recommendation engine now exists and generates real plans for new-shape users (see status above). Still open: the weekly-adaptation auto-regulation loop (`weekly_adaptation_responses` has no check-in UI yet; the engine only reads it defensively, holding the volume ramp after reported significant pain or a mostly-missed week) and the scheduled expert-discovery pipeline. Template content is generator-emitted from 7 blueprints — editing a blueprint and re-running `generate-templates.ts` is the authoring workflow; there's no admin UI for template content.
- ~~**Weekly workout-plan regeneration cron needs `CRON_SECRET`**~~ — **fixed 2026-08-12.** All three crons (`regenerate-workout-plans`, `regenerate-meal-plans`, `generate-weekly-reviews`) share this exact gate and were all silently 401ing in production since they shipped — `CRON_SECRET` was never set as a Vercel Production env var. Set it and verified all three working via manual invocation: workout/meal-plan regeneration ran for real users with stale plans (one pre-existing, expected failure each — a user who hasn't approved their training/nutrition parameters yet, unrelated to this fix), and the weekly-review cron generated a real Claude-authored brief end-to-end (`ai_runs` row, `weekly_reviews.status` → `generated`). The AI Summary feature specifically had **never once generated a real brief in production** before this fix, despite being fully built.
- **Meal plans have the same stale-lookup bug workouts just got fixed for**: `/plan/meals` still calls `getMealPlanForWeek(user.id)`, an exact `week_start = today` match with the same "only correct on the literal day of generation/approval" flaw described in the trainer-authored-programs entry above. The resilient fallback (`getActiveMealPlan`) already exists in `domains/mealplan/service.ts`, just isn't wired into that page yet. Next up.
- **Trainer-authored nutrition programs** (see status entry above) have no grocery-list/Sunday-prep cascade yet — an assigned program's meals reach `meal_plans`/`meal_plan_items`, but nothing downstream of that (grocery list, prep plan) regenerates from a trainer-materialized plan the way the client's own self-service flow cascades on approval. Also no per-date calendar/override UI (unlike the workout side) — a program's meals repeat identically every week within a phase, with no way to swap or override a single day yet.

## Accounts & services this project depends on

| Service | Used for | Where |
|---|---|---|
| Supabase | Postgres, Auth, RLS | project `Areta` (renamed from `Life OS`), ref `eqzrvidshghwvbycdvqi` |
| Vercel | Hosting, preview/prod deploys | project `Areta AI` (renamed from `lifeos`) under team `project-190` |
| Resend | Transactional email (signup confirmation, etc.) via custom SMTP | domain `areta-ai.com`, verified (migrated from `getlifeos.tech`, which was removed from Resend) |
| Vercel Domains | `areta-ai.com` registration + DNS | primary production domain; `getlifeos.tech`/`www.getlifeos.tech` and the `.vercel.app` alias are still attached to the same project too |
| GitHub | Source control, CI | `dgray4224/lifeos` (repo name not yet updated) |
| Anthropic | Weekly Operating Brief generation (`claude-sonnet-5`, forced tool-use) | API key in `ANTHROPIC_API_KEY`, set both locally and in Vercel production |

## Tech stack

Next.js (App Router) · TypeScript · Tailwind CSS · Supabase (Postgres, Auth, RLS) · Zod · React Hook Form · Anthropic SDK · Recharts · Vitest · Playwright · Vercel

## Project structure

```
app/            Routes. (auth) = login/signup/forgot-password/reset-password, (app) = authenticated shell (dashboard, onboarding, log/*, plan/*, review/*, settings/*), api/health-sync + api/exercise + api/onboarding = bearer-token endpoints for the mobile app (imported health data, the Exercise-tab swap feature, and mobile-native onboarding respectively)
platform/       Platform core: auth session/actions, Supabase clients, env validation, shared UI (incl. charts/, Card/Button/icons), theme/, AI provider (Anthropic + stub), calendar provider abstraction (Google/Microsoft/Apple + token-crypto)
domains/        Domain modules: identity, goals, nutrition, recovery, learning, coaching, onboarding, weight, sleep,
                exercise, exerciselibrary, workoutplan, trainingprogram, tasks, parameters, recipes, mealplan, grocery, prep, review, memory, prompts, motivation, account, calendar
supabase/       Migrations, dev seed data, local Supabase config, custom email templates
scripts/        seed.ts — dev-only founder profile seeder
tests/          unit/ (Vitest) and e2e/ (Playwright)
```

`platform/` is reusable and domain-agnostic; `domains/` holds one folder per product domain (schema + service, sometimes more). This split is intentional (see `CLAUDE.md` §20 rule 4) so domain build-out extends existing folders instead of restructuring.

## Local setup

1. **Install tooling**: Node.js (LTS), then enable pnpm via `corepack enable` (or `npm i -g pnpm`).
2. **Install dependencies**: `pnpm install`
3. **Get Supabase credentials**: from your Supabase project → Settings → API, copy the Project URL, `anon` public key, and `service_role` key.
4. **Configure env**: copy `.env.local.example` to `.env.local` and fill in the three values above, plus `ANTHROPIC_API_KEY` (from console.anthropic.com) if you want `/review/brief` to actually generate — without it, `getAIProvider()` falls back to a stub that throws instead of silently faking a result. Leave `ALLOW_SEED=false` unless you're about to run the seed script.
5. **Link and migrate**:
   ```
   pnpm dlx supabase login --token <a personal access token from supabase.com/dashboard/account/tokens>
   pnpm dlx supabase link --project-ref <your-project-ref>
   pnpm dlx supabase db push
   ```
6. **Regenerate types** (whenever the schema changes): `pnpm dlx supabase gen types typescript --linked > platform/db/types.ts`
7. **Run it**: `pnpm dev` → http://localhost:3000

Locally, `pnpm dev` still sends through Supabase's default shared mailer unless you also configure `SUPABASE_AUTH_EXTERNAL_*`-style local SMTP — in practice, just test signup/confirmation against the deployed app (which uses Resend) rather than fighting local rate limits.

### Auth email confirmation

Email confirmation is on, and the hosted project sends through **custom SMTP (Resend)** instead of Supabase's shared/default mailer — which was rate-limited to 2 emails/hour and explicitly not meant for production use. Setup:

- Domain `areta-ai.com` is verified with Resend (DKIM/SPF/DMARC records added via Vercel's DNS management, since the domain was bought through Vercel Domains).
- Supabase's SMTP settings (Authentication → Emails → SMTP Settings) point at `smtp.resend.com`, sending from `noreply@areta-ai.com`.
- Both the "Confirm signup" and "Reset password" email templates (Authentication → Emails → Templates) are custom branded templates (`supabase/templates/confirmation.html`, `supabase/templates/recovery.html`) — editable now that custom SMTP is on (Supabase locks template editing behind custom SMTP on the free/default mailer). **Both must be set up in the live dashboard to match the local `supabase/config.toml` definitions** — `supabase db push`/migrations don't sync email templates, only the Supabase dashboard (or Management API) does.
- Both templates deliberately link to our own `/auth/confirm?token_hash=...&type=...` route instead of Supabase's default `{{ .ConfirmationURL }}` link. The default link is a PKCE `code` that only completes if the *same browser/cookie context* that started the flow also opens the email link — fine for signup (usually opened right where you signed up), but a real bug for password recovery: on iPhone, tapping the reset link from the Mail app commonly opens it in an in-app browser with no access to that cookie, so the code exchange silently fails and the single-use token is burned before the user ever reaches `/reset-password`. `token_hash` + `verifyOtp()` has no such requirement — it works from any device/app that opens the link. (Found via Supabase auth logs: a real user's recovery link verified successfully server-side, then got hit two more times within a minute, both failing with "One-time token not found" — the classic signature of a token consumed by one browser context that a different context then tried and failed to reuse.)
- `signUpWithPassword` (`platform/auth/actions.ts`) passes `emailRedirectTo` pointing at `/auth/confirm`, and that route (`app/auth/confirm/route.ts`) handles both the PKCE `code` and `token_hash` formats generically — the same route also handles the password-reset link's redirect (`requestPasswordReset` points it at `/auth/confirm?next=/reset-password`), so it works regardless of which flow triggered it.
- If a confirmation or reset click ever fails, check Vercel logs for `[auth/confirm]` — the route logs the real Supabase error. One remaining known false alarm on the *signup* link specifically: email clients that prefetch/scan links (Gmail's Safe Browsing, etc.) can consume that single-use link before the user manually clicks it. Not a code bug; just click-once links being click-once.

Local dev's `enable_confirmations` is on in `supabase/config.toml`; if you run a full local Supabase stack (`supabase start`, requires Docker), confirmation emails land in the local mail-testing server at `http://127.0.0.1:54324` instead of going through Resend.

## Scripts

| Command | What it does |
|---|---|
| `pnpm dev` | Start the dev server |
| `pnpm build` | Production build |
| `pnpm typecheck` | `tsc --noEmit` |
| `pnpm lint` | ESLint |
| `pnpm test` | Vitest unit tests |
| `pnpm test:watch` | Vitest in watch mode |
| `pnpm test:e2e` | Playwright e2e (needs a running app + local Supabase via Docker) |
| `pnpm seed` | Seeds the dev-only founder profile (`CLAUDE.md` §17) — refuses to run unless `ALLOW_SEED=true` |
| `pnpm run content:add -- --spec <path>` | Validates + credibility-checks a drafted training-program content batch and generates a migration file — see [`docs/training-content-pipeline.md`](./docs/training-content-pipeline.md) |

## Testing

- **Unit** (`tests/unit/`): Zod schema validation for every onboarding and log domain, the onboarding-answers → structured-output transform (`domains/onboarding/transform.ts`), deterministic calculation logic across every phase (nutrition parameters, meal/grocery/prep-plan generation, weekly metrics, the prompt trigger catalog), env validation, and the `optionalNumberValue`/`optionalStringValue` form helpers (see Form patterns below). Run with `pnpm test`. The AI call itself (`AnthropicProvider`) isn't unit tested — it's a thin, mostly-untestable wrapper around a real API call; its Zod-validated output contract is what's tested indirectly by exercising `weeklyBriefSchema` and by manual end-to-end runs against the real API.
- **E2E** (`tests/e2e/`): a Playwright smoke test covering signup → email confirmation → onboarding → dashboard. Requires `supabase start` (Docker) running alongside `pnpm dev`; not yet run in this environment. Every other UI flow was verified manually via browser automation against both local dev and production instead.

## Form patterns worth knowing

React Hook Form + Zod's `.optional()` doesn't mean what you'd expect for blank form fields — this bit us twice during Phase 1 and both are now fixed with reusable helpers in `platform/ui/FormField.tsx`:

- **Optional numbers**: `valueAsNumber: true` turns an empty input into `NaN`, not `undefined`, which fails `z.number().optional()`. Use `register(name, { setValueAs: optionalNumberValue })` instead.
- **Optional selects**: a blank `<option value="">` submits `""`, which fails `z.enum([...]).optional()` (only `undefined` counts as "not answered"). Use `register(name, { setValueAs: optionalStringValue })`. This one is sneakier because the validation error can go unnoticed if the field's `error` prop isn't wired up — the form just silently refuses to submit with no visible feedback. Always pass `error={errors.<field>?.message}` to `FormField` for every registered field, including optional ones.

## Deployment

Vercel is connected to this GitHub repo (project `Areta AI`, formerly `lifeos`, under `project-190`) and auto-deploys every push to `master`, with `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, and `ANTHROPIC_API_KEY` set for Production (the first three also for Preview). GitHub Actions CI (`.github/workflows/ci.yml`) runs typecheck, lint, unit tests, and a build on every push and PR. The calendar-integration env vars (`GOOGLE_CALENDAR_CLIENT_ID/SECRET`, `MICROSOFT_CALENDAR_CLIENT_ID/SECRET`, `CALENDAR_TOKEN_ENCRYPTION_KEY` — see `.env.local.example`) are **not yet set** in Vercel; the app runs fine without them (Google/Microsoft just show "Not available" in Settings), but nothing calendar-related works in production until they're added and the app redeployed.

**Env var changes require a redeploy** — Vercel snapshots environment variables into each deployment, so adding/changing one (e.g. via `vercel env add`) only takes effect on the next deploy. Trigger one with `vercel --prod` (or just push) after any env var change.

The app is currently aliased to four domains: the primary `areta-ai.com`, the legacy `getlifeos.tech` (apex redirects to `www.getlifeos.tech`) and `www.getlifeos.tech` itself, and the Vercel-issued `lifeos-eosin-nine.vercel.app`. The legacy domains are still live and not yet redirected to `areta-ai.com` or canceled — see Known gaps. Supabase's Auth → URL Configuration Site URL is `https://areta-ai.com`, and the Redirect URLs allow-list includes `http://localhost:3000/**`, both `.vercel.app` production/preview patterns, `areta-ai.com`/`www.areta-ai.com`, and the legacy `getlifeos.tech`/`www.getlifeos.tech` (left in place so the old domain's auth flows don't break while it's still live).

If you add a new migration, remember to run `pnpm dlx supabase db push` against the linked project — Vercel does not do this automatically.

## Development rules

See `CLAUDE.md` §20 for the full list. The short version: deterministic code for calculations, AI only for interpretation/generation (Phase 4's `AnthropicProvider` and the weekly-motto quote selection are the two places this happens — see `platform/ai/` and `domains/motivation/`), validate all AI output (Zod schema, with a retry on failure — see `platform/ai/anthropic-provider.ts`), no one user's data baked into platform logic (see `supabase/seed/dev-seed.ts` for how the founder's dev profile is kept isolated), require user approval before any generated plan goes live.

**Active navigation state must change color, not just weight/border.** An active link, tab, or pill always gets brand-colored text (`text-brand`) — a bolder font weight or an underline alone reads as barely-there, especially on desktop where there's no separate tab-bar chrome to lean on for context. This was a real bug: the desktop top nav (`AppHeader.tsx`) had no active-state logic at all, and the Health-section domain pills (`Overview`/`Nutrition`/`Exercise`/`Sleep`) looked identical no matter which one was selected. Fixed by centralizing the rule in `app/(app)/nav-links.ts` — `navLinkClass`/`navTabClass`/`navPillClass` — so every nav in the app (`AppHeader`, `BottomTabBar`, `SettingsNav`, the dashboard section tabs and `DomainNav` pills, the Plan Week/Month/Year tabs) answers "am I active?" the same way instead of each inventing its own className. Any new nav added later should use one of these three helpers rather than a one-off active/inactive className.
