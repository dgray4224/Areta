/**
 * Placeholder until a real Supabase project is linked.
 *
 * Regenerate against the actual schema with:
 *   pnpm dlx supabase gen types typescript --linked > platform/db/types.ts
 *
 * `Database = any` keeps `createClient<Database>()` type-safe-by-intent
 * without hand-maintaining table shapes that the CLI will overwrite anyway.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
